#ifndef _HELP_H
#define _HELP_H

/*
  Procedure..: run_help
  Description..: Runs the help command and determines what command's help needs
  to be printed
  Params..: char* commandBuff, int bufferSize
*/
int run_help(char * commandBuff, int bufferSize);

#endif
