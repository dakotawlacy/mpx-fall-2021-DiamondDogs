#ifndef _SETDATE_H
#define _SETDATE_H


/*
  Procedure..: run_setdate
  Description..: Runs the setdate function that writes the date the user enters into
  the registers
  Params..: char* commandBuff
*/
int run_setdate(char * commandBuff);

#endif
