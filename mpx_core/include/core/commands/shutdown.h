#ifndef _SHUTDOWN_H
#define _SHUTDOWN_H


/*
  Procedure..: run_shutdown
  Description..: Runs the shutdown function that confirms that the user
  wants to exit the program
  Params..: char* buffer, int size
*/
int run_shutdown(char* buffer, int size);

#endif
