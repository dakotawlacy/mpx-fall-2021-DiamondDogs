#ifndef _KEYBOARD_CAPTURE_H
#define _KEYBOARD_CAPTURE_H

/*
  Procedure..: keyCap
  Description..: Handles all the keyboard input logic, letters, numbers, delete, backspace
  Params..: char* buffer, int location, int length
*/
int keyCap(char* buffer, int location, int length);

#endif
